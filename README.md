# YouTube-agents

<!-- Ahoy, matey! This repo now includes a TypeScript prime-number console voyage. -->

Learning to build custom agents from YouTube videos.

## Prime Numbers Console App

<!-- Arrr! Run these commands to hoist the sails and print the prime bounty. -->

1. `npm install`
2. `npm run build`
3. `npm start`

The app prints every prime number between 1 and 100, one per line.
